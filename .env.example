# =====================================================
# MMR Pipeline 환경 변수 예시
# 실제 값은 .env 로 복사한 뒤 수정해서 사용 (커밋 금지)
# =====================================================

# -----------------------------------------------------
# 1) Database (현재 사용 - DB 접근 버전)
# -----------------------------------------------------
DB_HOST=localhost
DB_PORT=5432
DB_NAME=league
DB_USER=postgres
DB_PASSWORD=changeme

# -----------------------------------------------------
# 2) Backend API (TODO: 추후 전환 예정 - 현재 미사용)
# -----------------------------------------------------
# API_BASE_URL=https://api.example.com
# API_TOKEN=

# -----------------------------------------------------
# 3) 출력 경로
# -----------------------------------------------------
OUTPUT_DIR=./output
