# CLAUDE.md

이 파일은 Claude Code와 기여자가 이 저장소(`gmok_mmr`)에서 작업할 때 따르는 규칙을 정의한다.

---

## 프로젝트 개요

- **gmok_mmr**: 리그 오브 레전드 내전 MMR 계산 서비스 (Python).
- **운영 형태**: FastAPI 기반 **계산 전용** 서버. 백엔드(`loltrix_be`)와 **HTTP/JSON으로만** raw 데이터/결과를 주고받는다.
- **아키텍처**: 데이터 엔지니어링 **메달리온** (silver → gold → serving).

### 디렉터리 구조

```
src/mmr/                # 운영 패키지 (계산 + API, DB 의존 없음)
├── silver/             #   정제 · 파생 feature · Game Impact
├── gold/               #   baseline · ELO MMR 계산/요약
└── serving/            #   service · api_server · schemas
tests/
├── harness/            # 테스트 전용 DB 입출력 (운영 미사용, 백엔드 연동 후 삭제)
├── cli/                # 로컬 실행 스크립트 (배치/baseline/full_mmr)
└── test_*.py           # 단위 테스트
docs/                   # 설계 · 로직 · 인터페이스 · 테이블 정의 문서
migrations/             # DB DDL
scripts/run_api_server.py   # 운영 API 서버 실행
```

---

## 브랜치 / PR 규칙

- **`main` 직접 push 금지.** main 반영은 `dev → main` PR로만.
- **`dev` 직접 커밋 지양.** `dev`에서 기능 브랜치를 따서 작업하고, 완료 시 **`dev`를 base로 PR**을 올린다.
- 브랜치 네이밍: `feat/*`, `fix/*`, `docs/*`, `refactor/*`, `chore/*`.
- 훅·서명 우회(`--no-verify` 등) 금지. 사용자가 명시적으로 요청한 경우만.

---

## 작업 워크플로우 (기능별)

모든 기능 작업은 다음 4단계를 따르고, **각 기능은 대응하는 `docs/` 문서를 가진다.**

1. **설계** — 접근 방식·영향 범위·트레이드오프 정리
2. **docs 정리** — 설계 내용을 `docs/`에 먼저 기록
3. **구현** — 코드 작성 + 테스트
4. **docs 정리** — 구현 결과로 문서 갱신 (파일 경로·산식·동작이 코드와 일치하도록)

> 기능마다 "설계 문서"가 남아야 한다. 구현만 하고 문서를 비우지 않는다.

---

## 코드 규칙

- **운영 패키지(`src/mmr`)에 DB 직접 접근 코드를 두지 않는다.** DB/로컬 배치는 `tests/harness`·`tests/cli`에만. 운영은 백엔드 API로만 입출력한다.
- 공개 API는 `mmr` 패키지 최상위(`__init__`)에서 재노출한다.
- 파일 이동/이름변경은 **`git mv`로 이력을 보존**한다.
- 라이브러리/서비스 코드에서 `print` 대신 `logging` 사용을 권장한다.
- 주변 코드의 스타일·네이밍·주석 밀도를 따른다.

---

## 문서 규칙

- `docs/`는 항상 **현재 소스 기준**으로 최신화하고, 파일 경로·산식 참조를 정확히 유지한다.
- `docs/interface_spec.md`는 백엔드↔MMR **통신 계약(SoT)**이며 **미래 계약 문서**다. 현재 구현과 다르더라도 임의로 현재 구현에 맞춰 덮어쓰지 않는다.
- 파생지표는 **반올림하지 않는다**(full precision). `game_duration` 분 환산만 소수 둘째 자리 반올림.
- 문서·커밋 메시지는 **한국어**.

---

## 테스트

- 실행: `python -m pytest -q`
- 변경 **전** 그린 베이스라인을 확인하고, 변경 **후** 회귀가 없는지 확인한다(사전 실패가 있으면 개수를 기준선으로 보고).
- 리팩터는 **골든값을 테스트로 고정한 뒤** 교체한다.

---

## 커밋

- **기본 커밋 컨벤션을 지킨다.** 제목은 `<타입>: <요약>` 형식, 타입은 `feat` / `fix` / `docs` / `refactor` / `chore`.
- 제목은 간결하게 쓰고, 필요하면 본문에 **무엇을·왜 바꿨는지 기본 내용**을 적는다. 요약 없는 빈약한 메시지는 금지.
- 커밋·푸시는 사용자가 요청할 때만 한다.
- 커밋 메시지 말미에 다음 트레일러를 유지한다:

  ```
  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
  ```
